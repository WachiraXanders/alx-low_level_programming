#include "main.h"
/**
 *mul(int a, int b)
 *@a: One of the numbers being multiplied.
 *@b: The other number being multiplied.
 *
 *Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	return (a * b);
}
