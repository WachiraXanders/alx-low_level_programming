#include "main.h"

/**
 *_isdigit - Check for a digit.
 *@c: Is the digit being checked.
 *
 *Return: Returns 1 if c is a digit .Otherwise 0.
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
