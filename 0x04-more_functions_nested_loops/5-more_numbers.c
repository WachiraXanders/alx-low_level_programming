#include "main.h"
/**
	*more_numbers - will print more numbers.
	*
	*/
void more_numbers(void)
{
	int a;
	int b;

	for (b = 0 ; b < 10 ; b++)
	{
		for (a = 0 ; a <= 14 ; a++)
		_putchar('0' + a);
	_putchar('\n');
	}
}
