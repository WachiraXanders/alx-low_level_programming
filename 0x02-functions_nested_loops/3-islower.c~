#include "main.h"
/**
	*_islower - will calibrate and find any lower case letters.
	*
	*Return: If c is lowercase then 1 . Else return is 0 .
	**/
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	return (1);

	else
	return (0);
}
